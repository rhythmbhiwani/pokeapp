import FullPageError from "@/components/full-page-error";
import React from "react";

function NotFound() {
  return <FullPageError message="The page you are looking for is not found" />;
}

export default NotFound;
